import 'package:flutter/material.dart';

class ImageOnlyTab extends StatelessWidget {
  const ImageOnlyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}